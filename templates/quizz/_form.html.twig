<div class="container">
    <div class="row">
        <div class="col-xs-12">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-success">{{ button_label|default('Sauvegardez') }}</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>
