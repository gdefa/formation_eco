<div class="container">
    <div class="row">
        <div class="col-xs-6 justify-content-center">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-success">{{ button_label|default('Sauvegarder votre leçon') }}</button>
            {{ form_end(form) }}

        </div>
    </div>
</div>

