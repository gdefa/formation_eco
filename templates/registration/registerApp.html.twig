{% extends 'base.html.twig' %}


{% block title %}S'inscrire{% endblock %}

{% block body %}
<div class="container">
    <div class="row align-items-center">
        <div class="col-xs-1"></div>
        <div class="col-xs-4 d-flex justify-content-center">
            <h2>
                INSCRIPTION APPRENANT
            </h2>
        </div>
        <div class="col-xs-6">
            {{ form_start(registrationForm , {
                'attr' : {'class' :  'mb-3 mt-3 font-weight-bold text-uppercase ' ,}
            }) }}
            {{ form_row(registrationForm.email , {
                label: 'E-mail :',
                'attr': {'class': 'mb-3'}
            }) }}
            {{ form_row(registrationForm.plainPassword, {
                label: 'Mot de passe :',
                'attr': {'class': 'mb-3'}
            }) }}
            {{ form_row(registrationForm.pseudo, {
                label: 'Pseudo :',
                'attr': {'class': 'mb-3'}
            }) }}
            <div class="mb-3"><button class="btn btn-primary d-block w-100" type="submit" style="color: var(--bs-white);background: #cda67a;">S&#39;inscrire</button></div>

        </div>
        {{ form_end(registrationForm) }}
        </div>
        </form>
        <div class="col-xs-1"></div>
    </div>
</div>
{% endblock %}


